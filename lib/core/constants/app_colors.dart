import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Colors
  static const Color primary = Color(0xFF4CAF50); // Green for sustainability
  static const Color secondary = Color(0xFFFF9800); // Orange for warmth
  static const Color accent = Color(0xFF8BC34A); // Light Green

  // Background Colors
  static const Color lightBackground = Color(0xFFF5F5F5);
  static const Color darkBackground = Color(0xFF121212);
  static const Color lightSurface = Colors.white;
  static const Color darkSurface = Color(0xFF1E1E1E);

  // Text Colors
  static const Color textPrimaryLight = Color(0xFF212121);
  static const Color textSecondaryLight = Color(0xFF757575);
  static const Color textPrimaryDark = Color(0xFFE0E0E0);
  static const Color textSecondaryDark = Color(0xFFB0B0B0);
  static const Color textDisabled = Color(0xFFBDBDBD);

  // Status Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFFB300);
  static const Color info = Color(0xFF2196F3);

  // Role Colors
  static const Color donor = Color(0xFF4CAF50); // Green
  static const Color recipient = Color(0xFFFF9800); // Orange

  // UI Elements
  static const Color divider = Color(0xFFE0E0E0);
  static const Color iconLight = Color(0xFF212121);
  static const Color iconDark = Color(0xFFE0E0E0);
}
